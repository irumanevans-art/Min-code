# 更新日志

每次发版在最小位加 1（`1.0.x`），除非这次改动明确说要升次版本。

## 2.1.23 — 2026-09-24

- **滚到顶时，第一条消息不再被顶栏洗淡**：顶栏后面那层纸色渐隐以前一直盖到按钮下沿，会话滚到顶时第一行字是浅影。现在到按钮行中线就收干净了，圆钮之间的字照旧被托住。
- **深色模式下你说的话看得清了**：你的消息那只气泡和会话底色只差一阶，夜里几乎融成一片。现在再浅一阶，一眼能扫到。
- **设置页的分隔线不再带毛边**：两端淡出的地方以前会漏出一条蓝色纹理细线，现在是干净的淡出。
- **电池优化那行不再从词中间断开**：「后台进程」以前被挤成「后台进 / 程」两行。说明现在占满整行，「去设置」单独一行靠右。
- 其它：会话列表的「⋯」菜单、搜索条、`@` 补全这三处，Claude 和 Codex 以前各写一份，现在各收成一处，行为不变。

## 2.1.22 — 2026-09-24

- **会话刚启动时状态条就有用量**：以前刚启动的那一次拉用量经常超时，要等第一轮对话结束才出现。CLI 是一条一条处理控制请求的，刚启动时算用量要几秒，而页面在握手完成前就把用量和计划连着要了两遍，后面的请求跟着一起超时。现在同一种刷新同时只发一条，握手期间攒着、握手完统一补。
- **托管的后台服务一启动就挂，模型也知道了**：以前模型收到的是「已托管」，服务其实已经死了（命令不存在、依赖没装），它还会让你去开预览。现在等服务出结论（最多 10 秒）再告诉模型：跑起来了、还是刚启动就退出了（带退出码和最后几行日志）、还是根本没起来（带原因），让它自己去修。等待期间按了停止、或换了档，迟到的应答不会发给已经换掉的进程。顺带，托管的那条命令不再被说成「被权限规则拦截」。
- **输入框里以 `!` 开头的一行在本地直接运行**：和终端里 Claude Code 的 `!` 一样，不经模型、不花钱。输出贴成一张卡片，并随下一条消息交给模型，所以 `!git status` 之后说「把这些改了」是通的。中文输入法打出的全角「！」也认。
- **终端顶栏多了个键盘开关**：打开终端时要不要自动弹键盘。关掉之后终端整屏都能看，点一下终端照样弹。默认开，和以前一样。
- **「系统」里点进去，返回还回到「系统」**：终端、进程、环境与更新、设置以前一点开就把抽屉关掉，返回时直接落到会话上。现在它们盖在抽屉上，抽屉停在「系统」这一页，关掉面板或返回都回到这里。
- **淘宝 npm 源只剩一处，而且记得住**：以前「环境与更新」里的勾选每次打开都是关的，设置里另有一个开关却管不到更新。现在勾选存在设置里，安装向导用的也是它；设置里那个重复的开关去掉了，「连接与安装」改叫「连接」，电池优化挪到了「设备」。
- **安装包小了约 22 MB**：标题和输入框用的霞鹜文楷取了子集（GB2312 全集加标点符号），缺的字由系统字体补上。

## 2.1.21 — 2026-09-23

- **App 更新找得到「下载并安装」了**：更新说明很长、或者手机字号调大时，说明把对话框撑满，底下的「取消 / 下载并安装」被挤没了。现在按钮固定在对话框底部，说明在上面滚动。
- **Claude Code CLI 的更新键只在真有新版时可点**：以前没点「检查更新」、或者已经是最新时也能点，点了只是把同一版原样重装一遍，版本号纹丝不动，看上去像更新失败。现在没检查过时按键写「先检查更新」，已是最新时写「已是最新（版本号）」，都点不了；安装不完整时的「修复安装」照旧随时可点。
- **Codex 的 `@` 补全和 Claude 一样好用了**：只打一个 `@` 就列出工作区的文件；可以按路径搜（`docs/d` 找得到 `docs/deep/d.txt`），以前只比文件名、单独一个 `@` 什么都不出。文件页的搜索不受影响。
- **更多写法的后台服务有预览位了**：`node -e "….listen(8798)"`、`php -S localhost:8000`、`gunicorn --bind 0.0.0.0:8001`、`flask run -p 5000` 这类命令起的服务，以前认不出端口、托管了也没有预览。
- 其它：会话管理器里「向 CLI 发控制请求、等应答、超时」这部分拆了出来，行为不变（启动、切模型、切权限模式、换档、用量都走它），并补了超时等测试。切模型、切权限模式、查用量如果和以前不一样，请反馈。

## 2.1.20 — 2026-09-23

- **按停止键不再冒红字「任务失败」**：回答输出到一半按停止，已经出来的内容留着，下面是一条灰色的「已中断」；消息刚发出、还一个字没出就按停止，消息退回输入框，对话流里不留痕迹。以前两种情况都会挂一条红字「任务失败: error_during_execution」。
- **托管的后台服务一启动就挂了，会直说**：比如 rootfs 里没装 python3，以前照样显示「已托管」、预览位弹出一页「网页无法打开」，模型还会说服务在跑。现在对话流里会出一条红字，写明退出码和原因（如「exit 127，命令不存在：python3: command not found」），工具卡标成出错；预览位要等服务真的跑起来才弹。
- **模型不存在这类失败不再把同一句话说两遍**：CLI 已经在回复里说明了原因时，红字只写「任务失败（原因见上）」。
- 其它：会话管理器里「读写 CLI 进程、停止时关进程」这部分拆了出来，行为不变；为此补了一套用真 CLI 录下来的对话回放的测试（普通回复、权限请求、子 agent、报错、打断、停止、换档重启）。停止会话、换档、托管后台命令如果和以前不一样，请反馈。

## 2.1.19 — 2026-09-23

- **会话抽屉改成两层**：供应商 / Codex / 工作区文件直接放在第一层，其余（复制整段会话、终端、进程、环境与更新、设置）收进「系统 ›」的下一页；在第二层按返回回到第一层，关掉抽屉再打开会回到第一层。

## 2.1.18 — 2026-09-23

- **跟进 Claude Code 2.1.280 和 Opus 5.5**：模型说明补上 `claude-opus-5-5`（每百万 token $4 / $20，1M 上下文，思考常开），`opus` 别名不再写死成 Opus 5——2.1.280 起它就是 5.5。项目里有 `AGENTS.md` 时不再提示「还没有 CLAUDE.md」（CLI 2.1.277 起会读它）。在「环境与更新」里把 CLI 更新到 2.1.280 即可。
- **设置改成分层页面**：首页是「连接与安装 / 设备 / 外观与语言 / 关于」四个入口，点进去是各自的页面，不再在一页上展开收起；连接那一行直接写着正在用的供应商。会话抽屉底部的入口也不再折叠。
- **通知里的「允许」只批准它显示的那一条**：以前待批请求换了一条而通知没刷新时，按下去批准的可能是一条你没看见的命令。现在通知跟着请求刷新，按钮只认它自己那一条。
- **App 更新**：更新说明按 Markdown 显示，不再是一堆 `###` 和 `**`；先下载、到安装时才要「安装未知应用」权限，授权后回到 App 自动接着装；GitHub 接口限流（同一出口 IP 一小时 60 次，运营商网络很容易碰到）时改走网页查，查不到会直接说限流了。去掉了容易误点、会跳到浏览器的「打开 Release 页」。
- **复制有提示了**：QQ 号、崩溃报告、整段会话复制后都会说一声（vivo 等系统自己不提示）。
- **长回答流式输出更顺**：不再每个字都把整段回答重新解析、把整页重画一遍。
- **Codex**：输入框草稿按顺序落盘，快速打字后被杀不会少字；新会话里没发出去的字，App 被杀后点「新会话」还在；app-server 的警告显示原文，不再只是「未知的 codex 方法：warning」；停止 Codex 不会因为管道写满卡住界面。
- **卡死的 Claude 会话能停干净了**：停止时进程自己不退，就整棵进程树一起收掉（以前只发 SIGTERM，proot 扛得住，进程会一直留着）。停止或换档时也不再误报「写入会话失败」。
- **密钥失效后「清除并重新配置」连统一供应商一起清**：以前清完统一供应商仍然打不开、也改不了，提示却消失了。
- 其它：清后台后接回虚拟屏更快；若干内部整理与测试。

## 2.1.17 — 2026-09-22

- **设置页收成三组折叠**：连接与安装 / 设备 / 外观与语言，默认只展开连接，不再一长串平铺。
- **会话抽屉底部入口分组**：引擎与供应商 / 工作区 / 系统，可折叠，默认展开引擎。
- **虚拟屏文案去掉「不用装 Shizuku」**：标题就是「虚拟屏」，说明里只保留重启后要再开无线调试。
- **关于页加联系作者**：QQ `1114111189`，点按复制。

## 2.1.16 — 2026-09-22

- **清后台后虚拟屏还能接上**：壳进程本来就不会被 `force-stop` Min 杀掉；现在壳每 3 秒重交一次 Binder，App 再打开会自动 `listAgentDisplays` 把还在的屏接回会话，不必重新 adb。重启手机后壳没了，仍要再激活一次。

## 2.1.15 — 2026-09-22

- **真虚拟屏在 vivo 上跑通了**：shell 壳服务用 `getContentProviderExternal` 交接 Binder（绕开 ContentResolver 的 package 校验），用 `com.android.shell` 的 Context 建 `FLAG_TRUSTED` VirtualDisplay（绕开 `packageName must match calling uid`）。Surface 接 ImageReader，主屏无浮窗。
- **隔离实测**：Settings 跑在 displayId=3，Min 仍在 displayId=0；`overlay_display_devices` 为空。
- LocalSocket 交接在 SELinux 下失败（Permission denied），已弃用。

## 2.1.13 — 2026-09-22

- **关于页可以检查 App 更新了**：查 GitHub Releases（`irumanevans-art/Min-code`），有新版本就下载 APK 并交给系统安装器。和 CLI 维护面板那颗「检查更新」不是一回事。Release 资产请按 `Min-code-<version>-<abi>.apk`（或 `universal`）命名。
- **DeepSeek「连不上」**：测活把 `/v1/models` 的 404/405 误当成连不上；DeepSeek Anthropic 兼容基址本来就没有模型列表。现在会显示「通了 · 没有模型列表」。同时 DeepSeek / MiniMax / 智谱 / Kimi Code 预设补齐官方模型映射与上下文 env；Kimi Code 按文档走 `ANTHROPIC_API_KEY`。
- **供应商编辑页**：新增「上下文量」快捷芯片（200k / 256k / 786k / 1M），手填供应商也能设；缺主模型映射时给非阻断提示。旧档案不会被静默改写——预设是一次性模板，重建或手补即可。
- MiniMax 国内基址改为文档上的 `api.minimax.cn`（原先 `api.minimaxi.com`）。

## 2.1.12 — 2026-09-22

- **自托管 shell 壳服务（不用另装 Shizuku）**：新增 `PrivilegedServer`，用 `app_process` 以 shell uid 跑起来，专门建 **TRUSTED VirtualDisplay**（Surface 接 ImageReader，不投物理屏）并在指定 display 上启动 Activity。设置页「虚拟屏」档可复制 adb 命令，或在打开无线调试后尝试本机拉起。
- **重启代价写进 UI**：副题标明「每次重启后需要再开一次无线调试」——这和官方 Shizuku 一样，fork 省掉的是另装 App，不是激活步骤。
- **没就绪就退回真屏**：`device_open_app` 仅在虚拟屏会话活跃时走 `launchOnDisplay`，否则仍是原来的真屏路径。
- **注意**：壳服务的端到端真机验证还没跑完（adb 设备在收尾时掉线）。装上后请：`adb shell` 执行设置页复制的命令 → 看「虚拟屏已就绪」→ 开始会话 → 确认主屏无浮窗、`dumpsys display` 有 VirtualDisplay。

## 2.1.11 — 2026-09-22

- **滑动不再一次甩到底**：行程从区域高度的 60% 降到 35%，时长 300→450ms。真机上两次翻车都是这一下太猛——无障碍列表找 Min、设置列表找电池，中间的项一闪而过。工具描述也改硬了：写明「每次只翻约三分之一、不会甩到底；滑完树没变就试反方向；OEM 设置列表必须带 index」。
- **系统设置不再进 denylist**：拦 `com.android.settings` 之后连「打开设置看一眼电池」都做不到，而这正是手机端 agent 最常被要求做的事。真正不可逆的是支付和短信，那两档保留。模型实测 2 分 24 秒走完「设置 → 电池 → 电池健康与充电」，读出最大容量 83%、当前 80% 正在充电。
- **多屏前置修好了**（为虚拟屏铺路，单屏行为不变）：
  - `dispatchGesture` / `takeScreenshot` 都认 `targetDisplayId`，不再默认打到主屏——否则虚拟屏场景会「读的是副屏、点的是主屏」。
  - 读窗从 `rootInActiveWindow`（跟焦点走）改成 `getWindowsOnAllDisplays` 按 displayId 取；你碰一下主屏不会把 agent 的读屏拽走。
  - 整屏滑动的尺寸按目标 display 算，不再拿主屏的 1260×2800 去打一块可能只有 800×1600 的副屏。

## 2.1.10 — 2026-09-22

真机（vivo V2359A / Android 15）跑通设备操控的完整闭环，顺带修掉四个**只有真机能暴露**的问题。上一版那套东西编译和单测全过，但一行都没在设备上跑过——下面每一条都是插上线之后才冒出来的。

- **新增 `device_screenshot`，作为读屏失败时的兜底**：UI 树只对原生控件有效，游戏、图表、自绘界面、部分 WebView 在无障碍里就是一块空白。以前这种情况只回一句「没读到」，模型会认为这件事做不到、或者反复重试同一个调用。现在 `device_ui_tree` 读到的元素少到不像真实界面时（≤3 个）会主动说「这个界面可能是自绘的，用 device_screenshot 看一眼」。截图走无障碍自带的 `takeScreenshot` 而不是 MediaProjection——后者每次都要弹系统授权框、还要拉一个前台服务，而无障碍权限里本来就含这个能力。长边压到 768：一张 1260×2800 原图是四千多视觉 token，压完一千出头而字仍认得清，不压的话「兜底手段」会变成「每用一次烧掉半个上下文」。**不做自动降级**——读不到就悄悄返回一张图，会让每次读屏的成本变成一个模型自己都不知道的数。

- **修：点击报成功却没生效**。Compose 的按钮在无障碍树里普遍是 `isClickable == false` 的裸 `View`（点击通过 AccessibilityAction 注册，不反映在那个属性上）——Min 自己的图标按钮就是这样。旧逻辑看到 false 就往上找可点祖先，一路找到铺满整屏的根容器，它确实 clickable、`performAction` 也返回 true，于是我们报告「已点击」而屏幕纹丝不动。现在先**无条件**在目标节点自己身上试一次 `ACTION_CLICK`，再找祖先且**祖先面积不得超过目标的 8 倍**（撑大几倍是正常嵌套，撑大几十倍就是整页容器），最后才拍坐标。报成功却没生效是最坏的失败模式：模型会据此往下走，而它脚下的屏幕根本没动。

- **修：点击生效了，下一次读屏却还是旧画面**。这条和「不订阅事件流」是一对：无障碍框架的节点缓存靠 `typeWindowContentChanged` 之类的事件失效，而我们为了不收集用户在每个应用里的一举一动只订了窗口切换。两者叠加的结果是抽屉明明已经打开、读回来的还是点击前那棵树，模型以为没点动又点一次。现在连上时 `setCacheEnabled(false)`，每次取根节点再 `refresh()` 一遍。代价是每次读屏多几毫秒跨进程，换来读到的一定是此刻的屏幕。

- **修：`device_open_app` 会把自己锁死**。它原先也查当前前台应用，于是人停在系统设置页时（`com.android.settings` 在名单里），agent 连「打开别的应用」都被拒绝——而那恰恰是离开那个界面的唯一办法。打开应用是**离开**当前界面，站在哪儿无关紧要，现在只查目标应用。tap / input / swipe 仍然查前台，那几个是「在当前这个应用里动手」。

- **修：工具里没接住的异常会让 CLI 干等**。`takeScreenshot` 在配置缺 `canTakeScreenshot` 时**直接抛** `SecurityException` 而不是走 `onFailure` 回调，异常一路冒到 socket 线程，那次请求连错误响应都回不出去，调用方只拿到一个空连接——看不出是超时、崩溃还是协议不对。现在 server 的兜底 catch 会回一条 JSON-RPC 错误，至少模型知道这次失败了可以换条路。（`canTakeScreenshot` 也补上了。）

- 另外记一条给用的人：vivo 上**侧载应用的无障碍服务根本不出现在列表里**，不是文案里说的「开关是灰的」。系统认得这个服务（`dumpsys accessibility` 里查得到），只是受限设置把它藏了。要先去「应用信息 → ⋮ → 允许受限设置」。

## 2.1.9 — 2026-09-22

- **agent 能操作这台手机了**：设置页新增「让 agent 操作这台手机」。开了之后 CLI 多出六把工具——读屏幕、点击、输入、滑动、打开应用、返回键，走 Android 无障碍服务，不需要 root，也不需要 ADB。选无障碍而不是 Shizuku 是因为后者在无 root 时**每次设备重启都要重新用 ADB 激活**，那个门槛会劝退绝大多数人；无障碍在系统设置里开一次就永久有效。工具挂给 CLI 的方式是 MCP，所以 Claude Code 和 Codex 两个引擎一行协议都没改就都能用。

- **读屏幕给的是结构，不是截图**：`device_ui_tree` 返回的是每个可交互元素一行的紧凑文本（`[3] Button "允许" (540,1893)-(980,2013) clickable`），不是图片。省掉视觉模型、token 便宜一个量级，而且点击目标是「第 3 个元素」而不是坐标 (540,1893)——不会因为分辨率、状态栏高度或弹窗偏移点空。纯布局容器会被扁平化（父容器没文字不等于要丢掉整棵子树），不可见子树整枝跳过，超过 120 个节点截断并告诉模型裁掉了多少。

- **点击优先用控件自己的语义**：`ACTION_CLICK` 而不是往坐标上拍——能正确触发 onClick、不受遮挡影响、不会因为动画位移点空。节点自己不可点时**向上找最近的可点祖先**：列表项里真正带 clickable 的常常是外层容器，而带文字、被模型认出来的是里面那个 TextView，不往上找的话模型每次都会挑中文字节点然后收到「点不动」。两条都不成立才退回拍坐标。滑动走 `dispatchGesture` 并**同步等它跑完**——不等的话模型下一步会读到动画中途的屏幕，然后基于一个不存在的界面继续操作。

- **银行和支付应用一律拦下**（思路抄自 [aster-mcp](https://github.com/satyajiit/aster-mcp) 的 PackagePolicyGuard，MIT）：两条规则照搬——**fail-closed**（认不出前台是哪个应用时拒绝而不是放行，因为认不出来的时刻恰恰是最不该盲目点下去的时刻），以及**读写分界**（读屏永远放行，点击输入才过闸；把读也拦掉会让模型连「我现在在哪」都答不上来，然后开始瞎猜，那比放行更危险）。拦的不是「危险应用」而是**误操作代价不可逆**的应用：设置里点错能改回来，支付应用里点错钱已经走了。短信同理——能读验证码 + 能点确认，等于绕开所有二次验证。

- **MCP server 只绑 127.0.0.1，且路径里带一次性 token**：同类开源项目（如 NeuralBridge）默认绑在设备的局域网 IP 上且不鉴权，那等于给同一个 Wi-Fi 下的任何人一把遥控你手机的钥匙。token 每次启动重新生成，所以每次冷启动都要重写一遍 CLI 的 `mcpServers`——不重写的话 CLI 读到的是上一次的死地址。关开关时会把那条配置删干净，留着的话 CLI 每次启动都白吃一次连接超时。

- **两处必须说清楚的话**：Android 13+ 对**侧载**应用默认把无障碍开关置灰，要先去「应用信息 → ⋮ → 允许受限设置 → 输 PIN」才点得动。Min 走 GitHub APK 分发，每个人都会撞上，所以这句话直接写在设置项的副题里——不写的话用户只会看到一个点不动的开关，以为功能是坏的。另外这个服务**不订阅事件流**（只订 `typeWindowStateChanged`，每次操作现读当前窗口）：订阅 `typeAllMask` 等于让系统把用户在任何应用里的每一次点击、每一个输入框变化都推给我们，那是我们根本不看、却实打实拿到手的隐私数据。

- **工具卡不再显示裸的 `mcp__min-device__device_tap`**：设备工具剥掉 MCP 前缀，摘要按工具类型给（「点击 [3]」「滑动 down」「打开 com.android.settings」）。其余 MCP 工具只去掉 `mcp__`、保留 `服务器__工具`——别家服务器的工具重名是常事（两个 server 都有 `search`），去掉服务器名就分不出来了。

- **内部**：HTTP 收发原语从 `LocalRelayServer` 提到 `core/relay/HttpWire.kt` 两个本机服务共用。起因是设备 MCP server 要起第二个服务，面前是「复制一份六十行的 HTTP 解析」还是「提出来共用」——复制的那份会和原件各自演化，某一天只有一边修了 `Content-Length` 的边界，而两边看起来都对。

## 2.1.8 — 2026-09-22

- **「在等你批准」不再伪装成「正在跑」**：两者在引擎里都是 `Running`，于是工具卡一律转圈——人从别的 App 切回来，满屏都在转，分不清哪张卡是机器在忙、哪张不点它就永远不动。现在轨道标记多一档语义：实心方 + **停住的定环**（涟漪扩到七成不再淡出），整段轨道的流动也一并停掉，折叠行右侧的转圈换成一句「等你批准」。和正在跑共用同一个词汇，差别只在动与静。不换色——朱留给判定的结果（拒绝、中断），等待本身还是海。这一档**不做成 `ToolCall.Status` 的第四个取值**：`tool_use` 和 `can_use_tool` 谁先到都有可能，权限请求先到时那张卡还没进列表，改状态就会漏；改成每帧从会话级真值派生，谁先到都对得上，也不必在每个 `pendingPermission = null` 的地方记得清回去。两个引擎共用。

- **可以把整台设备交给 agent 了**：设置页新增「让 agent 读写这台设备」。开了之后共享存储整个挂到 rootfs 的 `/sdcard`，CLI 里一个普通的 `cat /sdcard/Download/x.pdf` 就能读到——proot 里的进程本来就继承 App 自己的 UID，不需要任何桥接。默认关，而且开了还不够：得再去系统设置里给「所有文件访问权限」，两个条件都成立才真的挂，用户事后在系统里收回权限的话下一次起 proot 就不再挂。挂载点每次起进程现算，不是构造时定死的。**`Android/data` 与 `Android/obb` 仍然拿不到**，那是系统对所有应用的限制，文案里直说了，免得以为是坏了。

- **权限 sheet 现在说得清「要动的是哪个世界」**：proot 的 `-b` 挂不出只读，所以整机一开就是可写的，而原来的 sheet 只说「要用 Write 了」，不说写到哪去。现在路径分三档：`/workspace` 内不提；容器里的 `/etc`、`/usr` 这些提一句灰字（改坏了重装 rootfs 就回来）；落到 `/sdcard` 上的发朱字「这是你设备上的文件」——那一档改坏了没有 undo。碰到设备真实文件时**「始终允许」一条都不给**：那条规则会把往后所有同类写入都放过去，而这正是把整机交出去之后仅剩的那道闸。不解析 Bash 的命令行：从 `sh -c` 的字符串里认路径认不准，认错的代价是这条警示开始出现在明明安全的命令上，然后再也没人看。

- **系统「文件」App 里能直接打开工作区了**：注册了 DocumentsProvider，系统文件管理器和任意 SAF 选择器的侧栏里会多出两个入口——「工作区」（`/workspace`）和「Ubuntu 环境」（整个 rootfs）。任何第三方编辑器都能直接打开 agent 的工作区文件，不用再从文件页导出、改完再导入；新建、重命名、删除、复制到设备存储也都走通。`/dev`、`/proc`、`/sys` 排除掉——那几个是 proot 运行时才挂的，宿主这边只是空目录，列出来只会让人以为坏了。documentId 每次解析都过一道 canonicalPath 逃逸检查：注册成 exported 之后，路径就不再是自己跟自己说话了，`../` 和指向根外的符号链接都得拦住。rootfs 装好 / 重装之后会通知系统重读，不必等它自己想起来。

## 2.1.7 — 2026-09-21

- **MCP 服务器编辑页能打字了**：args / env / headers 三栏以前是受控字段——显示的文本由数据模型格式化出来，每敲一个键都走一遍「解析 → 回写 → 重新格式化」。于是 args 里打出的空格当场被拆参数吃掉，永远拼不出第二个参数；env / headers 里敲到一半的 `KEY=` 被解析成空 map、又回显成空串，每个键都从零开始。现象就是键盘像坏的，只有整段粘贴进得去。现在这三栏各留一份本地草稿：键入只改草稿、不解析也不回写，失焦那一下才解析回写；外部换了值（切了另一台服务器、自己刚回写完）而这一栏没有焦点时再把草稿对齐——解析是有损的（没有 `=` 的行会被丢掉），所以回写后立刻按落下去的那份重新显示，框里看到的正好是将要保存的内容。点保存也会先把焦点收掉再等一帧落盘，免得「填完直接点保存」把最后一次输入留在草稿里。

- **「不能移进自己的子目录」这条拦截真的生效了**：那句过滤写成了 `folder.path.startsWith("${'$'}{it.path}/")`，而 `${'$'}` 在 Kotlin 源码里就是一个字面的美元符号——于是拿「`${it.path}/`」这几个字符去跟真实路径比，永远不相等，这条防线从上线起就没拦住过任何一次，只能等真去移动时由后面的校验兜底报错。同一处的两句说明文案也因此把模板原文直接摆给人看。三处现在都改回正常的字符串模板。

- **看大图不再拿内存赌运气**：图片预览是整张解码的，现在随手一拍就是四千万像素，摊成位图就是一两百 MB 的瞬时占用，低端机在这里 OOM 只是早晚的事。现在分两趟：先只读尺寸、不解像素，按 2 的幂算出采样率把长边压到 2048，第二趟才真正解码，内存峰值按采样率的平方降下来。全屏预览这个用途上看不出分别。

- **供应商清单里的推广归因参数清干净了**：从 cc-switch 导入预设时本该剥掉的渠道参数漏了四处——火山引擎的 `ac`/`rc`、优云智算的 `ytag`（值里还带着 `cc-switch` 字样）、aicodemirror 的 `invitecode`。它们挂在每行「去拿 key」的链接上，点进去注册就会被算到某个渠道头上，正是导入脚本和 NOTICE 都声明过要杜绝的事。链接改成纯净地址，导入脚本的过滤表也补上这四个键——原先的 `code` 匹配不到 `invitecode`、`ref` 匹配不到 `rc`，才会漏。另外补回 2.1.0 改 NOTICE 时误删的一行：Ubuntu 镜像的来源披露没了以后，下面「Node.js from nodejs.org」那半句成了挂在上一条下面的孤儿。

## 2.1.6 — 2026-09-21

- **Anthropic 的轨道收笔改成「线长成结构」**：前两版一个是六道细芒、一个是八瓣实心星，都是把一枚徽章挂在线下面，和上面那条手拉的颤线不搭。Claude 登录页那幅画里，线是自己长成分子结构的——现在竖线收细落到一颗主节点，长出三根短杆、杆头各结一颗更小的节点，右上那根再分一级；杆接着线的粗细，颤线也颤到节点心上。釉淌进主节点，节点按相位依次亮起。
- **云的收笔改成三缕交缠下飘的雾丝**：旧版细钩在夜纸上没有面积，改成实心云朵又成了图标。灰白仙境那张图的云是白和灰萦绕出来的——现在最后一段雾散成三缕丝，从轴线上出发、相位错开地往下往右摆，途中彼此交缠、越远越细越淡，到底散尽；丝外两层软晕，没有硬边；夜纸上丝心再提一线浪沫。活着时整组慢慢飘。

## 2.1.5 — 2026-09-21

- **中转站自定义模型也能开 1M 了**：以前认不出家族的 id（`my-relay-model` 这类）会被当成「加 `[1m]` 没意义」，设置页的 1M 开关直接消失，还谎称「原生 1M」——结果菜单画出 `…/200k`，CLI 也按 200k 提前压缩。现在未知 id 跟 Haiku / 旧版一样走「加了才是 1M」：开关回来、选中值带 `[1m]`、潮环分母是 `1M`、启动 env 写入 `CLAUDE_CODE_MAX_CONTEXT_TOKENS=1000000`。无后缀仍按 200k，避免对真只有 200k 的自定义模型误报。
- **Anthropic / 云的轨道收笔重做**：陶那套末尾三道横向旋痕太跳——竖线突然变成横装饰。现在竖线连续收细落到一粒芯，从芯迸出六道不对称短芒（取 Claude 品牌星号的放射感，不做具象 logo）；釉仍淌进芯，芒按相位闪。云那套去掉「先沉再翘」的折返感：逸散薄雾更早离轴、几乎只上扬，末端更淡，碎絮间距拉开。

## 2.1.4 — 2026-09-21

- **粘贴 `https://…/v1` 不再测活失败**：OpenAI 兼容中转的地址常带着末尾 `/v1`，以前原样存进去，探活 / 取模型 / 会话目录都会拼成 `/v1/v1/models`——实测 404，而同一家在 cc-switch 里好好的。现在 Claude 侧入库、读出、拼路径都会剥掉末尾 `/v1`（中间路径里的不动）；Codex / OpenAI 侧相反，保留 `/v1`。机内路由拼上游地址也走同一套，不会再双写。

## 2.1.3 — 2026-09-21

- **只提供 `/chat/completions` 的中转终于能用了**：以前选了 DeepSeek / GLM / Kimi / 千帆 / SiliconFlow 这类，Claude Code 直接去敲一个它听不懂的方言，会话起不来。现在供应商编辑页可以标明「接口方言」——Anthropic Messages（默认，直连）、OpenAI Chat、OpenAI Responses。后两种会在机内起一个只听 `127.0.0.1` 的路由，把请求转成上游听得懂的形状（流式、工具调用、usage 都转），再把响应折回 Anthropic 的事件序列。Codex 那边选了 `wire_api = chat` 的中转同理，Responses ↔ Chat Completions 双向转。
- **路由的边界写在明面上**：地址是 `http://127.0.0.1:…`，只在 App 活着时通；路径里带一枚进程内随机 token，同机别的 App 拼不出来。settings.json 的托管区**不写**这个本地地址——那份文件可能被 App 外的脚本读，写进去等于把它们指向一个随时会消失的端口。终端页签和本地服务则和会话进程走同一个入口。
- **完整 URL 端点**：高级区可以勾「地址就是完整端点」，给路径不标准的网关用；只在走路由时生效。

## 2.1.2 — 2026-09-21

- **供应商列表长按拖动不再「闪但不走」**：前面挂了搜索框、提示、统一供应商区之后，拖动仍按 LazyColumn 的绝对下标去换位——那些固定项也占下标，于是常常越界直接没反应（被拖的那行只靠偏移在闪），偶尔又把拖动指针指到一个固定项上，旁边的行看起来在乱跳。现在按条目自己的 id 换位，固定项一律不参与。
- **复制一条供应商**：名字加 `copy`，插在原件下面，带上备注 / 环境变量 / 网站 / 方言，复制完不自动切过去——副本是拿来改的。
- **备注与搜索**：每条可以写一句「谁的号、哪档、什么时候到期」，显示在列表行上；列表顶上有搜索框，按名称 / 地址 / 备注过滤。搜索时不许拖，避免过滤后的索引写回整表。
- **取模型**：编辑页一颗按钮，问这家的 `/v1/models`，点一下就能把 id 写进主模型或 Haiku / Sonnet / Opus 那一档。这家没实现这个端点时只说一句，不拦任何操作。
- **接口方言与认证方式**：每条可以标明它说的是 Anthropic Messages、OpenAI Chat 还是 OpenAI Responses，以及 key 放进 `Authorization: Bearer` 还是 `x-api-key`。非原生方言会走机内路由（见下一版）；选了 x-api-key 之后会话进程和终端拿到的是同一个键——以前探活通、会话 401，多半就栽在这。
- **几颗常用开关**：子 agent 团队、工具搜索、思考强度拉满、关掉 Artifact。它们就是几个环境变量，底下那张表里看得见也改得动，没有第二份存储。
- **统一供应商**：一条同时供给 Claude Code 与 Codex。改它就重写它派生出来的条目；直接改派生条目就解绑，删的时候问清楚「一并删掉」还是「留着变成普通条目」。
- **深链导入**：认 `minc://v1/import?…`，也认 cc-switch 桌面端那套 `ccswitch://`。进来只开一个确认框，把名字、地址、key 的头尾摊开说，点头才入库——链接可以来自任何地方，而它带的是一个会被立刻拿去发请求的 key。

## 2.1.1 — 2026-09-21

- **三套风格的会话流，左边那条线不再是同一条**：换「云」换「陶」以前只换颜色——那条线的形还是海的：一样的平滑线身、一样的白沫、一样在末尾卷成一个浪卷。可浪会卷，云不会卷，陶坯上留下的是轮痕。现在三套各画各的：
  - 「海」不变：一道连续的水，末段向右卷成 1.45 圈的螺旋、螺心一粒海，生成时一粒白沫顺流钻进螺旋。
  - 「云」是聚散：线本身是一段段断开的雾，两端化开、中部最实，而且**一带浓一带淡**——少了这层，长短随机的段会平均成一条等距的虚线。末段先散成三粒越来越短的絮，再飘出两缕云：两头都收尖、起手就离开轴线（接在线尾上会读成折返，试过两版都栽在这），主缕长、次缕短而淡，缕外还有三粒碎絮。生成时没有粒子在跑，是一道透光自上而下掠过，扫到哪里哪里的雾就亮起来。
  - 「陶」是轮与手：线是手拉出来的，会轻轻颤、笔宽不匀，隔一段还有一处飞白（刻刀提了一下笔）。末段收口成三道越来越窄的同心旋痕，末了一粒泥点落在线走到的那一点上。生成时一滴釉缓缓下淌（比白沫慢、尾更长更柔），到底不进旋痕而是收进泥点；同时三道旋痕依次显影，像坯还在轮上转。
  三套共用的只剩标记（点 = 你、环 = 思考、实心方 = 正在跑…）和那套接缝几何——标记是语义不是风格。线身里所有「随机」都是按段序号算出来的定值，不是掷骰子：否则每次重组线都长得不一样，滚动时就是一条在抖的线。

## 2.1.0 — 2026-09-21

- **中转站换来换去，不用再重敲一遍 key**：以前连接配置只有「备注名 + 地址 + token」三个字段，换一家要自己记住地址、自己贴 key，而且中转常要的 `ANTHROPIC_SMALL_FAST_MODEL`、`API_TIMEOUT_MS` 这类变量根本没处填。现在它是一张能管的表：新开的「供应商」页里长按拖拽排序、每条各带一组自定义环境变量、点一下就切；切完空闲的会话自动重起并接上原来的上下文，**正忙的那个不动**，只在列表上打一个角标加一颗「重启以应用」——用户刚才只是点了一下列表，不该因此丢掉正跑的一轮。会话抽屉和 `/config` 面板里也各有一个就地换的入口，不必离开会话。
- **终端里敲 `claude` 终于有 key 了**：同一个 App 里，会话页能用的 token，换到终端页签就用不了——那个进程的环境变量里只有一个 `PATH`，一个凭据都没有。本地服务同理。现在两处都会带上当前供应商（可在供应商页关掉）。
- **内置了 87 家供应商的地址表**：官方、国内官方（智谱 / 月之暗面 / 深度求索 / 通义 / MiniMax…）、聚合中转、社区，按分类分组、可搜索，选一家就把地址和「这家的 key 长什么样」填好，只差粘一个 key。表来自 [cc-switch](https://github.com/farion1231/cc-switch)（MIT），但**联盟推广参数、赞助标记、以及按付费位排的文件顺序全部剥掉了**——列在这里只是方便，不等于背书。安装向导第一屏也能直接从这张表里挑。
- **配置能导出、能导回来、也能从现成的文件里认出来**：导出默认**不含 API Key**（`token` 这个键整个不出现，而不是写一个空串），要带 key 得单独确认一次，文件名会写明 `-with-keys`，并且直接写进你选的位置、绝不经过临时目录。导入一律**追加**：地址和 key 都一样的跳过，重名的加后缀，已有的条目一个字段都不动、当前生效的那家也不变，导完告诉你「新增几条、跳过几条、改名几条」。另外还能从 Rootfs 里现成的 `settings.json` / `config.toml` 认一条出来。
- **可以选择让 App 托管 Rootfs 里的配置文件（默认关）**：打开之后当前供应商会同步进 `~/.claude/settings.json` 的 `env` 块，给终端里手敲的 `claude` 和你自己的脚本用。它只动**自己写过的那几个键**，CLI 写的、你手写的一概不碰；换一家时上一家特有的键会被删掉，关掉开关时写过的全部清干净。第一次动那个文件之前会自动留一份底（只存在 App 内部，可以恢复）。**会话进程从不读这个文件**——那是无头模式下 CLI 的事实，界面上把这句话写出来了。
- **Codex 这边补齐了同样的能力**：存储层一直是一张表，可界面上永远只有一条——现在连接面板上半部分是那张表，下半部分编辑当前选中的那条。还加了 `wire_api` 的选择：以前写死成 `responses`，对只提供 `/chat/completions` 的中转来说等于这条配置永远连不上，而界面上完全看不出是为什么。另外修掉一个藏着的问题：编辑一条配置会把它挪到列表最后一行。Codex 的 key 仍然**只活在进程环境变量里**，永远不写进 `auth.json`，托管开关也不会改变这一点。
- **多了两套可选的界面风格，一共三套**：
  - 「海」——原来那套蓝。
  - 「云」——原本是 Codex 页专属的灰白仙境，现在是全局可选的；喜欢它的人终于能在 Claude 页用了。
  - 「陶」——Anthropic 自己的暖陶土色，新做的一张纹理：多倍频噪声叠两次域扭曲再加叶脉与锐化，最后把明暗分布逐档对齐到原来那张蓝底（均值 148.8 对 148.2，标准差 27.8 对 27.7），于是所有沿用至今的透明度和对比关系原样成立。
  换风格和换昼夜走同一道动画：一条细金边推出新纸色，遮住的那一瞬换掉主题，再露出来。会话内容左边那条线、发送键、每一个描边组件都跟着换纹理——它们本来就是从同一张纹理上镂空出来的。陶土那套里「判定的朱」单独往深冷推了一档：陶土的强调色本身就是橙红，照搬原来的朱，一个错误标记和一个主按钮就分不出来了。
- **顺手省了内存**：以前无条件解两张纹理常驻（约 10 MB），现在只解当前这套的那一张，换风格时放掉旧的——加了第三套反而更省。

## 2.0.1 — 2026-09-20

- **Codex 有了自己的一套界面，不再是 Claude 的影子**：两个引擎以前共用同一套「海」的蓝，进错页面自己都分不清在哪儿。现在 Codex 整页走一套灰白的「云」——底纹是把桌面那张灰白仙境压成的纹理，先把画面里的暗色石块整片抠掉补平，再把它的明暗分布逐档对齐到原来那张蓝底（均值 147.9 对 148.2，各分位差不超过 1），于是所有沿用至今的透明度和对比关系原样成立，只是颜色从蓝变成了灰白。会话内容左边那条线、发送键、每一个描边组件都跟着换了，因为它们本来就是从同一张纹理上镂空出来的——换纸不换刀。深色模式下同样一套。
- **Codex 正忙的时候也能接着打字**：以前一轮没跑完，整个输入框是灰的，想好的下一句只能自己记着。现在照 codex 自己的做法排队：发出去的先排着（界面上能看见、点一下能取回来改），上一轮**干净跑完**才自动放下一条。失败或被你按停的那一轮不会继续放——否则一个 401 能把排着的消息一条条烧光。
- **Codex 的输入框补齐了**：能加文件附件（删除前会问一声）、`@` 能补全工作区里的文件路径、`/model`、`/effort`、`/new` 直接在输入框里敲。模型和思考强度也从"连接配置"挪进了会话里——要换个模型不必退出去改设置再回来。图片附件还没做。
- **审批和会话恢复的一串修复**：一个审批还没答、又来一个时，通知现在会重新响（以前后来那个悄无声息）；审批的请求 id 原样回传，整数就还是整数；重开 App 恢复的是"最后一条真有内容的"会话，不再摆一条空的出来；设置里解密失败不再顺手把存着的密钥清掉。
- **英文界面不再冒出中文**：斜杠命令的中文说明那个开关，只在中文界面下出现。整张对照表的前提就是"界面中文、CLI 说明是英文"，界面切成 English 之后前提没了——而且光藏开关不够，之前存下的选择还会继续生效，得连表一起停掉才关得掉。
- **卡顿与构建**：设置面板打开时的磁盘读写挪出了主线程；语法高亮的锁从只锁缓存改成锁整段，避免同一份内容被并发改坏；启动画像重新采集，并且只收冷启动那一段——以前滚动点击也算进"启动路径"，两份画像一字不差，dex 布局那点优化等于白做。依赖上删掉了 `mavenLocal()`（本机能编、别人编不出的典型来源），并把 app 主题真正依赖的 material 库从 proot 模块挪回 app 自己。

## 2.0.0 — 2026-09-19

- **Codex 成了第二个能真用的引擎**：以前它只是个半成品页面——发第二条消息，第一条的回答连同中间跑过的命令一起消失；命令的输出会混进对话气泡里；转个屏或退出页面，正跑着的一轮就被杀掉。现在它和 Claude Code 是对等的两条路：入口在侧栏（终端、进程的旁边），会话流、工具卡、diff、审批面板全都是同一套渲染，不是另画一份。
- **Codex 终于连得上网了**：随 App 的 Ubuntu 是最小系统，里面**一张根证书都没有**。Claude Code 感觉不到，因为 Node 把证书编进了自己的程序；Codex 是另一种语言写的，走系统证书，于是每一次请求都断在握手之前——界面上只看到任务毫无动静地失败。现在安装时直接用 Android 自己的证书库（145 张），不必联网去装。同一条路上还修了 API key 根本没被发出去的问题：官方 provider 只认落盘的登录文件，改成自定义 provider 之后，key 全程只活在进程环境变量里，不写进磁盘。
- **切出去接个电话，Codex 还在跑**：它和 Claude 的进程一样是 App 的子进程，系统一旦把 App 挪进后台就会连锅端。现在它接进了同一套保活：只有 Codex 在跑也会撑起前台服务，常驻通知里写明它在忙还是空闲，等你批准时也会说；通知上的「停止全部」现在真的包括它——以前通知消失了，它那棵进程树还在后台跑着。
- **重开 App 还能接着上次聊**：Codex 自己把每条会话写成文件，但以前没人读它——每次进来都是白纸。现在进页面就看到上次说到哪儿（**只读文件，不会替你开进程**），「继续」接着那条线程聊、上下文还在，「新会话」另起一条。侧栏那个列表能翻回更早的会话，点一条只是摆出来看，要接着跑得再按一下「继续」。
- **界面不再中英混排（第二批）**：斜杠命令面板、环境与更新、会话设置、输入框和「+」菜单、首页那句标语——约 190 条文字以前写死在代码里，切成英文界面纹丝不动。这批全部搬进语言资源，两份语言逐条对齐。其中几处不是换个写法就行：权限模式的说明藏在底层代码的枚举里，思考强度、花费、折叠行的摘要混在能被测试覆盖的计算逻辑里，都先把"算什么"和"怎么说"拆开了再搬。
- **粘贴一大段文字的占位符改成了固定写法**：它由程序生成、发送前再换回原文。以前这行字是中文的，于是在中文界面粘贴、把 App 切成英文再发送，程序就认不出自己写的占位符——发出去的是一行乱码，原文彻底丢失。现在它不随语言变（`[paste #1 · 87 lines · 3.4k chars]`），切语言之后照样还原得回来。

## 1.1.14 — 2026-09-18

- **终端里终于能用 `claude` 和 `node`**：终端页签自己拼了一份启动命令，漏了把 Node 的目录放进 PATH，而它开的又是非登录 shell，环境配置也不会被加载 —— 于是同一个 Linux 环境，Claude 自己执行命令时 `claude` / `node` / `npm` 都在，你在终端里敲却是 command not found。两处各写各的拼装现在收口成一处，故意的差异（比如后台服务要独立于会话存活）用参数表达，不是抹平。
- **停止后台服务，真的停得掉了**：以前点「停止」只结束外层的沙箱进程，里面的 node / npm 会变成没人管的孤儿 —— 端口还占着、CPU 空转、但已经不响应任何请求，而界面上显示的是「已退出」。现在先自底向上把里面的进程全部结束，再收外层。（顺带绕开两个 Android 平台坑：系统的「强制结束」其实只发了温和信号，内核也不提供列出子进程的接口。）
- **换思考强度时打的字不会再凭空消失**：切档要重启进程再续上会话，那 4~8 秒里界面还显示「运行中」、发送键照样能点 —— 发出去的消息会被写进一个已经关掉的管道，对话流里留下一条看着已发出、模型却从没见过的消息，没有任何提示。现在这段时间发的话先排队，新进程接上后自动送出。同一次修掉另外两种：追加消息后马上切档，可能让模型把同一句话回答两遍；切档时有任务在跑，会让发送键永远卡成中断键、只能按 Esc 解。
- **明文 HTTP 中转会提醒你**：地址是 `http://` 且不是本机时，设置页常驻一条提醒 —— key 和全部往来内容都以明文经过公网，路上任何节点都能看到、也能改写。**只提醒，不拦截**：已经在用的配置照常工作，不弹窗、不中断；只有新填或改成另一个明文地址时问一次「仍然使用」。本机地址（localhost / 127.x / 模拟器回环）走 http 不再误报，局域网地址仍然算不安全。
- **普通命令不会再被误当成长驻服务**：判断「这条是不是要长期跑的服务」以前是在整条命令里找关键词，于是 `cat vite.config.ts`、`pip install uvicorn`、`grep uvicorn logs.txt` 全都命中 —— 在手动确认模式下直接被拒，你连「批准一次」的机会都没有。现在只在命令位置匹配，管道、`&&`、`sudo` / `nohup` 包装、`npx` / `python -m` 都会先解开；拿不准就当不是。
- **界面不再中英混排**：侧栏、安装向导、会话页里有一批文字写死在代码里，切成英文界面时纹丝不动。这批已搬进语言资源（三处共 98 条），中英两份一一对应。剩下的几处下个版本继续。
- **系统镜像下载会校验了**：以前不校验哈希，官方源失败还会静默换成镜像站；文件名里又写死了小版本号，上游一发新的小版本就 404。现在安装时先取官方校验清单、按当前小版本下载并逐字节核对，对不上就明确失败，不再降级成「装上了但不知道装的是什么」。
- 深色模式默认改成跟随系统（你自己选过的不受影响）。
- 一批看不见但要命的修复：回放历史会话时不再误存编辑快照；快照改为同步读取，避免快的工具改完了才存、把改后的内容当成原样；同一条命令并发启动时不再重复起两个服务；后台协程出错时会话会明确标成失败，不再永远显示「运行中」；通知 id 从 16 位哈希换成 SHA-256，避免两个会话撞号、把权限批给错的那个；新建页签时后续请求不再被整批丢弃；工作区搜索的 glob 补上了越界过滤。

## 1.1.13 — 2026-09-17

- **重开历史会话不再乱弹预览**：以前每次打开老会话，回放重建界面时会把历史工具输出里的 localhost 链接重新扫一遍塞进预览位——每次都当第一次自动展开，弹开一个早就没有服务在监听的地址（连接被拒）。历史里的 Bash 命令在同一条路上也可能被重新托管成本地服务（此前没出事只是靠执行顺序侥幸）。现在回放只画卡片，不做任何副作用。

## 1.1.12 — 2026-09-17

- **退回输入框的消息再也不会半路丢掉**：启动失败、握手超时、切换失败、会话已关闭时，排队消息会退回输入框；但以前碰上输入框没在屏幕上（切了后台 / 页面没开着），退回的文字会悄悄蒸发，对话流里还留着「已退回」的提示。现在没人接就写进草稿盘，下次打开这个会话时原样躺在输入框里。启动中按停止也一样：排队的消息先退回再停，不再留永远「排队中」的幽灵条目。
- **崩溃横幅不再误报**：被后台协程接住、App 实际没崩的异常，以前和真崩溃写同一个文件 —— 启动时竖起「上次运行崩溃过」的假横幅不说，还可能盖掉你没来得及看的真崩溃堆栈。现在两类记录分开存，横幅只认真崩溃，关于页里两份记录都能看到、都能复制。
- 修复撤销快照的目录消毒漏洞：工具 id 是 `.` 或 `..` 时消毒结果原样放行，快照会落到 checkpoints 根目录并且互相覆盖，撤销可能还原错文件；现在纯点 id 换成等长下划线。

## 1.1.11 — 2026-09-17

- 适配 Claude Code CLI 2.1.274：它从这版起不再为还在连接的 MCP 服务器等那两秒，被工具搜索延迟加载的工具要到后面某一轮才报上来。会话流里那条「会话已建立 · N 个工具」以前发出去就定死在首轮那个偏小的数字上，和顶栏对不上；现在数字长出来会就地改准。没配 MCP 的话本来就碰不到。

## 1.1.10 — 2026-09-17

- **API key 可以存多条**：设置 → 连接现在是一张表，每条是「备注名 + token + 中转地址」。点一下换当前生效的那条，右边那颗笔改内容。token 和地址绑在一起存 —— 换中转站本来就要同时换两样，分开存的结果一定是某次只换了一半。列表上 token 只露头尾。老版本的单个 token 会自动迁成第一条，不用重填。
- **长按会话里的字终于能好好选中**：刚在输入框打过字的时候，长按模型的回答会选不中，或者弹出来的菜单只剩一个按了没反应的「全选」、没有「复制」；切去别的界面再回来又好了。原因是键盘收起时那次"顺手清掉焦点"把选中刚拿到的焦点一起清了。现在只有焦点确实还在输入框上才清。
- **修复生成中的正文停在第一截不动**：Markdown 短文本解析出来的结果被丢掉了，同一条消息后续到达的文字不会重绘。
- **设置里的语言终于能换**：以前点「English」格子会变蓝、值也存下来了，界面却纹丝不动 —— 调的那个方法要求界面用 AppCompat 那一套，而 Min 是纯 Compose，于是它被调用时静默什么也没干。现在 Android 13+ 直接交给系统（顺带把「系统设置 → 应用 → Min → 语言」那个入口也点亮了），更低版本自己接管。改完立刻生效，不用重启。从系统那边改的语言也会同步回设置页。
- **预览槽不再乱弹端口**：工具输出里找「listening on … port N」这类提示的规则收紧了三处。一段 curl 回来的 CSS（`--wp-bound-block-color` … `max-width:100%`）曾被读成端口 100，预览槽弹出一个连不上的 `127.0.0.1:100`。
- 输入框上方那行标签不再换行压字：窄屏 + 放大字体时 `ANTHROPIC_AUTH_TOKEN` 会断成两行，第二行压进上一格的输入框里。

## 1.1.9 — 2026-09-16

- **打的字不再凭空消失**：会话启动失败、握手超时、切换思考强度 / 提示缓存失败时，还排队的消息原样退回输入框，改改就能再发；会话已经关掉时按发送也一样退回，不再留一条永远「排队中」的幽灵消息。
- **CLI 更新失败能自愈**：上次更新中途断掉会留下「外壳已是新版、原生程序还是旧版」的错配，而旧文件看着完好，再点更新永远被判成「已是最新」、旧版就一直跑下去。现在失败会留一个标记，下次更新强制重装原生程序。
- **修复工作区搜索越界**：工作区里放一个指向区外（比如 App 私有目录）的符号链接，搜索会把外面的文件内容读进结果；现在越界的命中直接丢弃。
- 后台 / 长驻命令进进程表失败时给出可见的报错、工具卡标成错误，不再静默跳过；已经进表的工具卡也不会被随后那条拒绝结果翻回错误态。
- 后台协程出错不再把整个 App 带崩：异常落进崩溃记录，关于页照样能看到。
- 修复关闭会话时 CLI 进程可能残留：停止流程必须走完才回收，不再被中途取消。

## 1.1.8 — 2026-09-16

- **追加的话，跑完这一步就听见**：生成中发出的消息不再攥到整轮结束才写出去，而是立刻交给 CLI，由它在下一个循环边界插进这一轮 —— 和终端里的手感对齐。实测（`tools/probe_queue.py`，CLI 2.1.272）：第一个工具刚回结果，模型下一句就在回应你追加的那条，整轮只有一个 `result`。停止键的三档语义没变：排队时按停止仍是「打断当前轮、排队那条接着跑」，只是那条现在由我们留的副本重发（interrupt 帧带 `cancel_queued`，CLI 侧队列会被清空）。
- 会话还没起来时打的字不再凭空消失：先攥着，握手把工作目录切好之后再发。换思考强度 / 缓存 TTL 要重启进程时，排队的消息跟着搬到新进程。
- stdin 改成单消费者串行写。以前两次 `launch` 落在不同线程上抢同一把锁，紧挨着发的两条消息在 CLI 那边的先后没有保证 —— 追加消息立刻出手之后，顺序必须真的算数。
- **工作区文件长按多选**：长按任意一行进入多选，单项菜单挪到行尾的 ⋮（一个不少）。选中后底栏换成导出 / 移动 / 分享 / 删除，顶栏是「已选 N 项」+ 全选 / 反选。多选时锁住横划，免得一个手势把选择栏晾在「环境」页上。
- **批量与整目录导入导出**。导出时问一句：**打包成一个 .zip**（连目录结构，空文件夹也留着），或者**复制到你选的文件夹**（逐个写出去，对方拿到的是散文件；SAF 写不了修改时间，这一点保不住）。导入同样两条路：系统目录选择器整棵拷进来，或者选一个 .zip 解到当前目录 —— 顶层散着两个以上名字时默认帮你套一个文件夹。文件夹终于能导出和分享了（以前 `exportFile` 只认单个文件，菜单里直接灰着）。
- 打包会跳过符号链接（rootfs 里有自指的链接，跟进去就不会停）、`.l2s.*` 和 proc/sys/dev/run；解压侧按实际写出的字节算上限并拒绝所有能跳出目标目录的条目名。**因此 rootfs 归档不是可还原镜像**，只能当文件级备份看。
- **打开方式**：文本、图片、其他应用三选一，可以勾「以后 .md 都这样打开」；记住的偏好在文件太大打不开编辑器时会自动退回默认路由，不会把人送进一个必然报错的界面。
- 往 `linux/` 区导入的入口收起来了 —— 那是系统盘，写进去太容易把环境弄坏；导出和打开方式仍然给。
- 分享用的缓存目录每次写之前先清空。以前分享一次 400 MB 的东西就永久占掉 400 MB。

## 1.1.7 — 2026-09-15

- **预览是「位」**：会话顶栏常驻预览键（终端左侧）。有本机 loopback 才有效；关掉是藏，不是拆。Agent 填 URL 可自动展开一次；进程表「打开」= 按这个键，不再第二套 WebView。
- **进表认后台意图**：主路径是 Bash `run_in_background`；dev-server 白名单只是薄启发式。`can_use_tool` 时后台/长驻 Bash **排他**进进程表并 deny CLI 再跑一遍，消端口双开。
- **身份卡**：后台用 flag、缺命令 `apt-get install -y` 真装、本地页走预览位；去掉「bare & dies」交通规则。会话 / 托管 env 带 `DEBIAN_FRONTEND=noninteractive`。
- **顺一点**：启动后台预解码海纹理 + BitmapShader 预热；会话流式时关掉列表 `animateItem`；进程日志 StateFlow 400ms 节流；代码高亮结果缓存；Baseline Profile（`profileinstaller` + `:baselineprofile` 采集）让首次交互走 AOT。
- 建议 Rootfs CLI ≥ **2.1.271**（MCP-only `-p` resume、headless 等修在 CLI 侧；stream-json 形状无大改）。

## 1.1.6 — 2026-09-15

- **一张进程表**：Agent 后台/长驻 Bash 由独立 proot 托管进 `LocalServiceRegistry`；关对话、换一轮，进程和端口还在。前台保活与「停止全部」覆盖会话 **和** 这张表。
- **应用内预览**：本机 `http://127.0.0.1:…` 默认在软件内 WebView 打开（Markdown / 终端 / 进程表），系统浏览器只是次要入口。不再教人抄局域网 IP。
- 侧栏「进程」= 表（停 / 日志 / 打开），不是第二种启动仪式。
- 会话注入设备 DNS + 短身份卡（`/root/.claude/CLAUDE.md`）；`/proc/net` 读不了就老实说读不了，不造假。

## 1.1.5 — 2026-09-14

- 平板分界线拖宽终于跟手：以前 `pointerInput` 的 key 绑了当前宽度，拖一帧就拆掉手势，看起来一次只能挪几毫米；拖动中也不再跟弹簧抢位置。热区加宽到 28dp。

## 1.1.4 — 2026-09-14

- 平板 / 横屏会话栏对齐 Kimi、DeepSeek：左上角可收起；按住分界线横拖调宽；向右拖满可盖住主区，再点菜单收回。
- 关于页更新日志按 `a.b.c` 归组：同一 `b` 下的补丁版叠在一起，同一 `a` 下的次版本同理；默认只展开最新一组。

## 1.1.3 — 2026-09-14

- 无头会话终于会自己拟名：`-p stream-json` 不会自动写 `ai-title`，首轮成功后主动调 CLI 的 `generate_session_title`（persist），列表不再一直停在第一条用户消息。
- 相册发图跟系统 Photo Picker 上限走，不再人为卡 4 张。
- 搜索只留在会话记录抽屉，并能按正文命中；进行页顶栏放大镜去掉。
- 切模型 / 思考强度时，设置面板顶部立刻转圈并写「正在应用…」，不再只是选项变灰。
- 界面语言：跟随系统，或在设置里强制中文 / English（`values` 英文默认，`values-zh` 中文）。

## 1.1.2 — 2026-09-13

- 会话列表终于能用上 CLI 自动拟的标题：以前只认 transcript 里的 `custom-title`（人手 `/rename`），自动拟名实际写的是 `ai-title`。现在两者都认，人手改名仍优先；回合结束后会再扫一次磁盘，等异步落盘。

## 1.1.1 — 2026-09-13

- 对照 Claude Code CLI 2.1.270：回合结束展示被权限规则拦截的工具（`permission_denials`）；Bash 改文件时展开工具卡能看 diff（`bashEditDiff`）；CLI 配置里可设思考强度上限 `maxEffortLevel`。
- 会话能搜了：抽屉按标题 / 分类 / id 过滤；打开的会话顶栏放大镜搜正文，上/下跳到命中并自动展开折叠的工作块。
- 建议把 Rootfs 里的 CLI 升到 2.1.270（中转站每轮 HTTP 400、headless 误报「等待中」、resume 热切模型丢回复等修在 CLI 侧）。

## 1.1.0 — 2026-09-13

- 更新 Claude Code 时，原生二进制跟着 wrapper 换版本：它是拷到 `bin/claude.exe` 再删掉平台包目录的，不是硬链接。以前文件够大就跳过，界面显示新版本、会话仍跑旧 native。
- 空框顺延改成会话真的打开之后才消耗。判定本身不再清指针，并发满了空框还能用。
- 文件页装 / 重装 Linux 环境和向导一样：官方源连不上换阿里云镜像；手填的地址仍只用这一份。
- `relaunchWith` 用来决定 `--resume` 还是 `--session-id` 的「磁盘上有没有 transcript」补了单测。

## 1.0.12 — 2026-09-12

- 结束会话、切模型、改权限 / 思考强度 / 工作目录等会等 CLI 的操作，一点就进入忙碌态：停止键转圈，副标题写「正在结束会话…」或「正在应用会话设置…」，设置面板里的选项暂时点不动，避免连点。
- 安全分类器或配额把模型降级后，界面上的模型跟着切到实际在跑的那个（只改本会话，不改新会话默认）；聊天流里的说明还在，也可以再手动切回去。
- 会话列表标题改成立刻用 CLI 自拟的名字（`custom-title`），不必等这一轮跑完、transcript 落盘；人手改过的名字仍然优先。
- 关于页的更新日志按版本号折叠，只有最新一条默认展开。
- 深色模式会话轨上的白沫改成沿蓝线弧长走的多粒短痕，拐进螺旋也不再冲出线条；直线段也看得出粒在连续往前。

## 1.0.11 — 2026-09-11

- 停止键现在等价于电脑上的 **Esc**，按官方终端的三档行为分派：
  本轮已经有输出就打断；请求刚发出、一个字都还没吐就**撤回**（对话流里摘掉那条消息，
  原文退回输入框）；有排队中的新消息就打断当前轮、立刻开跑那一条。
  为此追加消息改成由 App 自己排队（以前一发就进 CLI 的内部队列，再也拿不回来）；
  排队中的消息在对话流里标「排队中 · 按停止可撤回」。
- 输入坞下面那条状态行整条撤掉了。模型摘要、上下文、花费是「想起来才看一眼」的参考信息，
  却常驻占着手机上最金贵的一条横向空间，还逼着三样东西互相挤——「今日 … · 本次 …」两个金额
  都在时，用量能吃掉整行，停止键被量成 0 宽**直接从界面上消失**（这就是 sonnet 下按不到停止的原因）。
  现在它们收进输入框左边的「+」：上下文与花费一栏（一圈**潮环** + `880k/1M` + 两笔金额），
  会话设置一栏（点开就是原来的设置面板）。胶囊外面不再有任何一行。
- 停止键搬进胶囊里，紧挨着发送键。它没跟着收进「+」——埋进下拉菜单就要点两次，
  而「停下」必须一按就到。和发送分两个键而不是合成一个：生成中本来就可以追加消息。
- 上下文用量从一道 40 dp 的线改成**潮环**：十二点起手顺时针漫一圈，浪头一粒白沫，
  逼近上限整圈转朱砂。比例这种量画成圆比画成线更直接，也省地方。
- 会话页右上角加了**终端**：和会话同一个 rootfs、同一个 `/workspace` 的真 bash，
  盖在对话上升起，返回键落下。每个在跑的会话预开一个页签、落在它自己的工作目录里。
  （它是平行的 shell，不是会话进程的控制终端——会话跑的是无头 stream-json，没有 TTY。）
- CLI 进程写到 stderr 的每一行现在都能看到了，折叠成对话流里的「进程输出 · N 行」。
  以前只有「像错误」的行会显示，其余当噪声丢掉，真东西混在里面就再也查不到。
  红字的判定没变：可见不等于报警。
- 首界面 Min 的 i 上那颗太极去掉，退回字体自带的点——那枚记号压在流动的海上是唯一不动的东西。
  桌面图标和启动页的太极保留。

## 1.0.10 — 2026-09-10

- 修好工作目录切不动：给 CLI 的 `set_cwd` 参数名本该是 `path`，之前发的是 `cwd`，
  于是每次切换都被回「path must be a non-empty string」。首页和会话设置里选目录现在都真的生效。
- 目录要求确认信任时自动应答（沙箱里的目录本来就是用户自己选的），CLI 拒绝时按原因说人话。
- 工作区文件可以搜了：顶栏放大镜，按文件名搜当前目录往下的整棵子树。
- 启动界面 Min 的 i 上那个点改成太极，和桌面图标一致。
- 桌面图标：字放大，M 比 in 高（M 偏右上、in 偏左下），错位收到上一版的三分之二；
  i 的点正坐在海沙分界线上，太极的阴阳交界与那条分界线连成一条。

## 1.0.9 — 2026-09-10

- 桌面图标：M / in 的错位再收一半，字稍放大，仍是一整词斜体 Min。

## 1.0.8 — 2026-09-10

- 桌面图标：M 沿右上、in 沿左下只各移图标高度的 1/10；太极仍是原来 i 点的大小，阴阳交界贴着对角线。

## 1.0.7 — 2026-09-10

- 桌面图标：Cormorant Garamond 斜体；M 偏右上、in 偏左下，不再被圆角裁掉；i 的点落在对角线上，做成太极。

## 1.0.6 — 2026-09-10

- 桌面图标改成两色对角线：海 #2B7FD6 与沙 #EFE2C9，分界刚好是对角线；「Min」是衬线斜体，不再用手绘稿。

## 1.0.5 — 2026-09-10

- 工作区文件可以重命名、移动到其他文件夹；长按条目也会打开菜单，不再只有删除。
- 文件图标按种类区分：markdown、代码、图片、文档、压缩包、音视频不再长得一样。
- 占用空间边扫边报进度，并跳过 proc/sys/dev，不会再停在「计算中」毫无反应。

## 1.0.4 — 2026-09-10

- 首页工作区路径可以点进去选文件夹，不再是一行死字。
- 会话设置里的工作目录改成浏览工作区目录，不必手填路径。
- 选过的工作目录会记住，下次启动会话还在那个文件夹。

## 1.0.3 — 2026-09-10

- 会话列表标题改用 Claude Code 拟的名字（transcript 里的 `custom-title`），不再截第一条消息。
- 会话可以置顶、分类、重命名；删除会停掉内部进程并清掉 transcript。
- 深色模式顶栏 Min 的光晕改成夜纸色，不再是一圈白。
- 任务进行中仍可追加消息（发送键与终端一致，中断是旁边单独的停）。
- 续会话时带上上次的权限模式，plan 模式里结束的会话不会掉回默认。
- 对照 Claude Code CLI 2.1.267：不启用 `--permission-prompts none`；cwd 跨轮次保持。（后续基线见 1.1.1 / CLI 2.1.270）

## 1.0.2

- 「海」视觉：纸 / 墨 / 海；发送键那扇窗里的海在流。
- 多会话、草稿落盘、上下文用量与花费。
